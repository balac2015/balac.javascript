//
   Created by danne on 2015/10/24.
extends ../layouts/default
block header
  link( rel="stylesheet" href="/plugins/datatables/dataTables.bootstrap.css")

//应用的page title
block pageTitle
  h1 系统角色管理
    small 添加，删除，更新系统中管理角色
block main
  .box
    .box-header.with-border
      h3.box-title 角色列表
      .box-tools.pull-right
        a.btn.btn-box-tool.btn-default(href="/system/role/add")
          i.fa.fa-plus
          span  添加角色
        //button.btn.btn-box-tool(data-widget="collapse")
        //  i.fa.fa-minus
    .box-body
      table#roleList.table.table-bordered.table-hover.table-striped
        thead
          //th.text-right(width="40") #
          th(width="40%") 角色名
          th(width="40%") 描述
          th.text-center(width="80") 操作
        tbody

      form#roleDelForm(method='post', style='display:none', action='/system/role/del')
        input#ids(type='hidden',name='ids')
        input#_csrf(type='hidden', name='_csrf', value=_csrf)

//应用添加的脚本在scripts block中定义
block scripts
  script(src='/plugins/datatables/jquery.dataTables.min.js')
  script(src='/plugins/datatables/dataTables.bootstrap.min.js')
  script.
    $(function () {
      var opts = {
        "language": {
          "url": "/plugins/datatables/Chinese.json"
        },
        autoWidth: false,
        lengthChange: true,
        searching: true,
        processing: false,
        serverSide: true,
        deferRender: true
      };
      var t = $('#roleList').DataTable($.extend(opts, {
                ajax: {url: '/system/role/datatable'},
                order: [[0, 'asc']],
                columnDefs: [
                  //{targets: 0, visible: true, sortable: false, searchable: false, defaultContent: ''},
                  {
                    targets: -1, visible: true, sortable: false, className: 'text-center',
                    render: function (data, type, row) {
                      return "<a class='btn btn-default btn-xs edit' href='/system/role/edit?id=" + row._id + "'><i class='fa fa-edit'></i></a>"
                              + " <button class='btn btn-default btn-xs del' type='button'><i class='fa fa-trash-o'></i></button>";
                    }
                  }
                ],
                columns: [
                  //{data: ""},
                  {data: "name"},
                  {data: "description", defaultContent: ''},
                  {data: ""}
                ]
              }
      ));
      $('#roleList').on('click', 'button.del', function () {
        var id = t.row($(this).parents('tr')).data()._id;
        $("#roleDelForm #ids").val(id);
        if (id) {
          bootbox.confirm("确定要删除数据吗?", function (result) {
            if (result) {
              $("#roleDelForm").submit();
            }
          });
        }
      });
    });
